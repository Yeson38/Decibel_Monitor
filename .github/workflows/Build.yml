name: Build

on:
  push:
  workflow_dispatch:

jobs:
  build_app:
    runs-on: windows-latest
    name: Build
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Install .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.x

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-
      
      - name: Build
        run: pwsh -ep bypass ./tools/publish.ps1

      - name: Checksum
        run: pwsh -ep bypass ./tools/generate-md5.ps1 cipx/
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          path: |
            ./cipx/*.cipx
            ./cipx/checksums.md
          name: Decibel_Monitor-Actions

      - name: 🐛 诊断 checksum 文件
        run: |
          echo "===== 文件是否存在 ====="
          ls -lh ./cipx/checksums.md
          echo "===== 文件大小是否超限(125KB) ====="
          if [ $(stat -c%s "./cipx/checksums.md") -gt 125000 ]; then
            echo "❌ 文件过大，将被截断"
          else
            echo "✅ 文件大小正常"
          fi
          echo "===== 文件内容预览 ====="
          cat ./cipx/checksums.md    
          echo "===== 检查特殊字符 ====="
          file ./cipx/checksums.md
      
      - name: Upload APP to release
        uses: ncipollo/release-action@v1
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        with:
          artifacts: "./cipx/*.cipx"
          draft: true
          bodyFile: ./cipx/checksums.md
          token: ${{ secrets.GITHUB_TOKEN }}
